/**
 * @desc services.js contains all the API URLs
 */

const apiLocation = 'https://demo7175924.mockable.io'

export default {
  getProductsList: `${apiLocation}/get-products`,
  getSizeList: `${apiLocation}/get-sizes`,
}
